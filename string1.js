    let age=17
    if(age>=18){
        document.write("user can play pubg","<br>");
    }
    else{
        document.write("user cannot play pubg","<br>");
    }
    let num=0
    if(num){
        document.write("no is even","<br>");
    }
    else{
        document.write("no is odd","<br>");
    }

    let drink= age>=18? "can have Beer<br>" : "sorry no Beer<br>";
    document.write(drink)

    // turnary operator or conditional operator

    let days=10;
    let vegitables= days<=5?"goot to have <br>":"avoid<br>"
    document.write(vegitables) 

    